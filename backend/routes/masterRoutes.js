const express = require('express'); const router = express.Router(); const masterController = require('../controllers/masterController'); const { isLoggedIn, isMasterAdmin } = require('../middlewares/authMiddleware'); router.use(isLoggedIn, isMasterAdmin); router.post('/onboard', masterController.onboardCompany); router.get('/companies', masterController.getAllCompanies); router.put('/companies/:id/config', masterController.updateCompanyConfig); router.put('/companies/:id', masterController.updateCompany); router.delete('/companies/:id', masterController.deleteCompany); module.exports = router;
